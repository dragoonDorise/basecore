import React from "react";

export const Th = ({ children, dataTH }) => {
  // if (headValue === true) {
  //   return <th>{children}</th>;
  // } else {
  // console.log(dataTH);
  return <th>{children}</th>;
  // }
};
